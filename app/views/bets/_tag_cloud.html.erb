<div class="row panel callout radius">
	<h3 align="center"><u><i>Hottest Bet Topics</i></u></h3>
	<div id="tag_cloud">
	  <% tag_cloud Bet.tag_counts, %w[s m l] do |tag, css_class| %>
	    <%= link_to tag.name, tag_path(tag.name), class: css_class %>
	  <% end %>
	</div>
</div>